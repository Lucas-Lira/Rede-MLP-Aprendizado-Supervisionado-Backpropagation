package Modulos;

import java.util.ArrayList;
import java.util.List;

public class FuncoesGerais
{
    public static List<Double> buscaMaiorMenor(List list)
    {
        // Da para mudar o nome da função e criar sim modelos separados porem uma função que faz a chamda para não ficar duplicando código na interface principal
        if(!list.isEmpty())
        {
            List<Double> result = new ArrayList();
            //result.add(list.get)

            return result;
        }
        else
            return null;
    }
}
